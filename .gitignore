node_modules/
.env
*.local
service-tokens*.json
aegis.config.json

*.bak
aegis-audit.jsonl
telegram-state.json
ledger-backups/

# operator's uploaded background/inlay images + their settings: state, never code, and the
# reason they survive a rebuild, a redeploy and a deallocate/start
ui-state/
